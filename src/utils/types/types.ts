export type InputComponentProps = {
    text:string
}

export type ButtonComponentProps = {
    text: string;
    scale:string
}

export type CoordinatesType = {
  latitude: number;
  longitude: number;
};